import {
  REGISTRATION_APPROVED,
  REGISTRATION_ERROR,
  GET_USER,
  LOGIN_APROVED,
  LOGIN_ERROR,
  SING_OFF,
} from "../../types/index";

export default (state, action) => {
  switch (action.type) {
    
    default:
      return state;
  }
};
